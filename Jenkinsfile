pipeline {

    agent any

    stages {
        stage("build") {
            dir("${env.WORKSPACE}/Node") {
                sh 'pwd'
            }
        }

        // stage("test") {
        //     steps {
        //         sh 'npm test'
        //     }
        // }

        // stage("deploy") {
        //     steps {
                
        //     }
        // }
    }

    // post { // executes after all the stages are done

    // }
}